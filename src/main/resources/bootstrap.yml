spring:
  application:
    name: HystrixDemo
  profiles:
    active: dev
  cloud:    
    config:
      uri: http://localhost:8888
      
endpoints:
  refresh:
    enabled: true
  restart:
    enabled: true

management:
  security:
    enabled: false
